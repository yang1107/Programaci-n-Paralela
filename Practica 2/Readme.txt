Para la ejecución hay que indicar el tamaño (n) de la matriz desde la línea de comando.
mpirun -np [número de procesadores] [nombre programa] [n]

n tiene que ser múltiplo de número de procesadores.

